# Placeholder so HyDE's dynamic.conf `source = .../hyde.conf` resolves.
# hyde.conf is HyDE's legacy fallback config ("Fallback if they still use hyde.conf").
# It is normally absent, but HyDE sources it unconditionally; without it Hyprland
# shows a "source= globbing error: found no match" in the startup Error Overlay.
