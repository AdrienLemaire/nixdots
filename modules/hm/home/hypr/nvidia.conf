# Placeholder so HyDE's dynamic.conf `source = .../nvidia.conf` resolves.
# This is an Intel-only machine (i915), so no NVIDIA env/cursor settings are needed.
# HyDE sources this file unconditionally; without it Hyprland shows a
# "source= globbing error: found no match" in the startup Error Overlay.
